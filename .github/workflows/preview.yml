---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
    branches:
      - develop
      - beta
      - master

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: preview-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  DOCKER_HUB: uwucode/ygege

jobs:
  # Only build preview if PR has the 'preview' label
  check-label:
    name: Check if preview requested
    runs-on: ubuntu-24.04
    outputs:
      should_build: ${{ steps.check.outputs.labeled }}
    steps:
      - name: Check for preview label
        id: check
        run: |
          if [[ "${{ contains(github.event.pull_request.labels.*.name, '🧪 preview') }}" == "true" ]]; then
            echo "labeled=true" >> $GITHUB_OUTPUT
            echo "✅ Preview label found, will build Docker image"
          else
            echo "labeled=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No preview label, skipping Docker build"
            echo "💡 Add the '🧪 preview' label to this PR to build a preview Docker image"
          fi

  build-preview:
    name: Build preview Docker image
    needs: check-label
    if: needs.check-label.outputs.should_build == 'true'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      pull-requests: write
    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_BRANCH: ${{ github.event.pull_request.head.ref }}
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate preview tag
        id: tags
        run: |
          # Create a safe tag name from branch name
          SAFE_BRANCH=$(echo "${{ env.PR_BRANCH }}" | sed 's/[^a-zA-Z0-9._-]/-/g' | tr '[:upper:]' '[:lower:]')
          
          # Preview tag format: pr-123 and pr-123-branch-name
          echo "PR_TAG=pr-${{ env.PR_NUMBER }}" >> $GITHUB_OUTPUT
          echo "PR_BRANCH_TAG=pr-${{ env.PR_NUMBER }}-${SAFE_BRANCH}" >> $GITHUB_OUTPUT
          
          echo "🏷️ Will tag as:"
          echo "  - ${{ env.DOCKER_HUB }}:pr-${{ env.PR_NUMBER }}"
          echo "  - ${{ env.DOCKER_HUB }}:pr-${{ env.PR_NUMBER }}-${SAFE_BRANCH}"
          echo "  - ghcr.io/${{ github.repository }}:pr-${{ env.PR_NUMBER }}"
          echo "  - ghcr.io/${{ github.repository }}:pr-${{ env.PR_NUMBER }}-${SAFE_BRANCH}"

      - name: Build and push preview image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.DOCKER_HUB }}:${{ steps.tags.outputs.PR_TAG }}
            ${{ env.DOCKER_HUB }}:${{ steps.tags.outputs.PR_BRANCH_TAG }}
            ghcr.io/${{ github.repository }}:${{ steps.tags.outputs.PR_TAG }}
            ghcr.io/${{ github.repository }}:${{ steps.tags.outputs.PR_BRANCH_TAG }}
          build-args: |
            BUILD_COMMIT=${{ github.event.pull_request.head.sha }}
            BUILD_DATE=${{ github.event.pull_request.updated_at }}
            BUILD_BRANCH=${{ env.PR_BRANCH }}
          labels: |
            org.opencontainers.image.title=Ygégé Preview
            org.opencontainers.image.description=Preview build for PR #${{ env.PR_NUMBER }}
            org.opencontainers.image.source=${{ github.event.pull_request.html_url }}
            org.opencontainers.image.revision=${{ github.event.pull_request.head.sha }}
            preview.pr.number=${{ env.PR_NUMBER }}
            preview.pr.branch=${{ env.PR_BRANCH }}
          cache-from: |
            type=gha,scope=preview-pr-${{ env.PR_NUMBER }}
            type=registry,ref=${{ env.DOCKER_HUB }}:${{ steps.tags.outputs.PR_TAG }}
          cache-to: type=gha,mode=max,scope=preview-pr-${{ env.PR_NUMBER }}
          provenance: false

      - name: Comment on PR with preview info
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: |
            const prNumber = ${{ env.PR_NUMBER }};
            const prTag = '${{ steps.tags.outputs.PR_TAG }}';
            const prBranchTag = '${{ steps.tags.outputs.PR_BRANCH_TAG }}';
            const dockerHub = '${{ env.DOCKER_HUB }}';
            const ghcr = 'ghcr.io/${{ github.repository }}';
            const sha = '${{ github.event.pull_request.head.sha }}'.substring(0, 7);
            
            const comment = [
              '## 🐳 Preview Docker Image Ready!',
              '',
              'Your PR preview has been built and published. You can test this PR by pulling the Docker image:',
              '',
              '### Docker Hub',
              '```bash',
              `docker pull ${dockerHub}:${prTag}`,
              `docker pull ${dockerHub}:${prBranchTag}`,
              '```',
              '',
              '### GitHub Container Registry',
              '```bash',
              `docker pull ${ghcr}:${prTag}`,
              `docker pull ${ghcr}:${prBranchTag}`,
              '```',
              '',
              '### Quick Test',
              '```bash',
              '# Using Docker Hub',
              `docker run --rm ${dockerHub}:${prTag} --version`,
              '',
              '# Using GHCR',
              `docker run --rm ${ghcr}:${prTag} --version`,
              '```',
              '',
              '### Image Details',
              `- **Commit**: \`${sha}\``,
              `- **Branch**: \`${{ env.PR_BRANCH }}\``,
              '- **Architectures**: `linux/amd64`, `linux/arm64`',
              '- **Tags**:',
              `  - \`${prTag}\` (updates on every push)`,
              `  - \`${prBranchTag}\` (includes branch name)`,
              '',
              '### Using with Docker Compose',
              '```yaml',
              'services:',
              '  ygege:',
              `    image: ${dockerHub}:${prTag}`,
              '    ports:',
              '      - "8080:8080"',
              '```',
              '',
              '---',
              '<sub>🤖 This preview image will be updated automatically when you push new commits to this PR.</sub>',
              '<sub>📦 To remove the preview label and stop building images, remove the `🧪 preview` label from this PR.</sub>'
            ].join('\n');

            // Find existing preview comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });

            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('🐳 Preview Docker Image Ready!')
            );

            if (botComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment,
              });
              console.log('✅ Updated existing preview comment');
            } else {
              // Create new comment
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: comment,
              });
              console.log('✅ Created new preview comment');
            }

  cleanup-comment:
    name: Update PR when preview label removed
    needs: check-label
    if: needs.check-label.outputs.should_build == 'false' && github.event.action == 'unlabeled'
    runs-on: ubuntu-24.04
    permissions:
      pull-requests: write
    steps:
      - name: Comment about preview removal
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: |
            const prNumber = ${{ github.event.pull_request.number }};
            
            const comment = [
              '## ℹ️ Preview Docker Image Disabled',
              '',
              'The `🧪 preview` label has been removed from this PR. Preview Docker images will no longer be built.',
              '',
              '**Note**: Previously built preview images remain available on Docker Hub and GHCR until manually removed.',
              '',
              'To re-enable preview builds, add the `🧪 preview` label back to this PR.'
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: comment,
            });